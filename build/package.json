{
  "name": "home-automation",
  "version": "0.0.1",
  "devDependencies": {
    "gulp-load-plugins": "^0.10.0",
    "gulp-concat": "^2.5.2",
    "run-sequence": "^1.0.1",
    "del": "^1.1.0"
  },
  "scripts":{
      "build-debug": "gulp build-debug",
      "build-release": "gulp build-release && git add ../. && git commit -m '-' && git push",
      "build-watch-debug": "gulp build-watch-debug"
  },
  "private": true,
  "license": "proprietary",
  "dependencies": {}
}